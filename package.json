{
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm --workspace client run build && npm --workspace server run build",
    "start-server": "npm run build && NODE_ENV=production npm --workspace server start",
    "start-server-win": "npm run build && SET NODE_ENV=production && npm --workspace server start",
    "dev-server": "npm --workspace server run dev"
  }
}